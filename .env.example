# Public URL where users reach the instance.
PUBLIC_URL=http://localhost:8080

# Database connection string (used outside of docker compose).
DATABASE_URL=postgres://nottario:nottario@localhost:5432/nottario?sslmode=disable

# Local listening address.
HTTP_ADDR=:8080
